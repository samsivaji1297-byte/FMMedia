<section class="hero-section">
  <div class="hero-content">
    <div class="status-pill">Sovereign OS v2 — Empire Expansion Architecture</div>
    <h1 class="hero-title">Sovereign Systems & Digital Assets.</h1>
    <p class="hero-subline">
      Building an ecosystem of high-signal frameworks, automated pipelines, and independent digital assets.
    </p>
  </div>
</section>

<style>
  .hero-section {
    padding: var(--space-xl) 0 var(--space-lg) 0;
    border-bottom: 1px solid var(--border-color);
  }
  .status-pill {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
    border: 1px solid var(--border-color);
    padding: 0.25rem 0.75rem;
    border-radius: 99px;
    margin-bottom: var(--space-md);
  }
  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin-bottom: var(--space-sm);
  }
  .hero-subline {
    font-size: 1.25rem;
    color: var(--text-secondary);
    max-width: 650px;
  }
</style>
